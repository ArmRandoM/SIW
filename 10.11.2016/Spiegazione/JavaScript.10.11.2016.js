var pluto = prompt("Dal file inserisci valore");

var f1File = function(p)
        {
          var pippo;
          pippo = prompt(p + " : Inserisci un valore");
          alert("il valore di pippo è " + pippo);
        }
